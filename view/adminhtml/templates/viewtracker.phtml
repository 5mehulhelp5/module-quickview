<?php
/**
 * Copyright © Panth Infotech. All rights reserved.
 * View Tracker Dashboard Template
 *
 * @var $block \Panth\QuickView\Block\Adminhtml\ViewTracker
 */

$stats = $block->getViewStats();
$mostViewed = $block->getMostViewedProducts(10);
$recentViews = $block->getRecentViews(20);
$trendData = $block->getViewTrendData();
$hourlyData = $block->getHourlyDistribution();
$topCustomers = $block->getTopCustomers(10);
?>

<style>
    /* Tailwind-inspired CSS with beautiful design */
    .view-tracker-container {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
        background: #f8f8f8;
        padding: 2rem;
        min-height: 100vh;
    }

    .tracker-header {
        background: white;
        border-radius: 1rem;
        padding: 2rem;
        margin-bottom: 2rem;
        box-shadow: 0 10px 30px rgba(0,0,0,0.1);
    }

    .tracker-header h1 {
        font-size: 2.5rem;
        font-weight: 700;
        background: linear-gradient(135deg, #eb5202 0%, #f38b00 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        margin: 0 0 0.5rem 0;
    }

    .tracker-header p {
        color: #6b7280;
        font-size: 1.125rem;
        margin: 0;
    }

    .stats-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
        gap: 1.5rem;
        margin-bottom: 2rem;
    }

    .stat-card {
        background: white;
        border-radius: 1rem;
        padding: 1.5rem;
        box-shadow: 0 4px 6px rgba(0,0,0,0.07);
        transition: transform 0.3s, box-shadow 0.3s;
        border-left: 4px solid;
    }

    .stat-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 20px rgba(0,0,0,0.15);
    }

    .stat-card.purple { border-color: #eb5202; }
    .stat-card.blue { border-color: #3b82f6; }
    .stat-card.green { border-color: #10b981; }
    .stat-card.orange { border-color: #f59e0b; }
    .stat-card.pink { border-color: #ec4899; }

    .stat-value {
        font-size: 2.5rem;
        font-weight: 700;
        margin: 0.5rem 0;
    }

    .stat-card.purple .stat-value { color: #eb5202; }
    .stat-card.blue .stat-value { color: #3b82f6; }
    .stat-card.green .stat-value { color: #10b981; }
    .stat-card.orange .stat-value { color: #f59e0b; }
    .stat-card.pink .stat-value { color: #ec4899; }

    .stat-label {
        color: #6b7280;
        font-size: 0.875rem;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        font-weight: 600;
        margin: 0;
    }

    .stat-icon {
        font-size: 2rem;
        opacity: 0.3;
        float: right;
    }

    .content-grid {
        display: grid;
        grid-template-columns: 2fr 1fr;
        gap: 1.5rem;
        margin-bottom: 2rem;
    }

    @media (max-width: 1200px) {
        .content-grid {
            grid-template-columns: 1fr;
        }
    }

    .chart-card, .list-card {
        background: white;
        border-radius: 1rem;
        padding: 1.5rem;
        box-shadow: 0 4px 6px rgba(0,0,0,0.07);
    }

    .card-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1.5rem;
        padding-bottom: 1rem;
        border-bottom: 2px solid #f3f4f6;
    }

    .card-title {
        font-size: 1.5rem;
        font-weight: 700;
        color: #1f2937;
        margin: 0;
    }

    .badge {
        background: linear-gradient(135deg, #eb5202 0%, #f38b00 100%);
        color: white;
        padding: 0.375rem 0.75rem;
        border-radius: 9999px;
        font-size: 0.875rem;
        font-weight: 600;
    }

    .product-list, .customer-list, .recent-list {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .product-item, .customer-item, .recent-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 1rem;
        border-bottom: 1px solid #f3f4f6;
        transition: background 0.2s;
    }

    .product-item:hover, .customer-item:hover, .recent-item:hover {
        background: #f9fafb;
    }

    .product-item:last-child, .customer-item:last-child, .recent-item:last-child {
        border-bottom: none;
    }

    .item-info {
        flex: 1;
    }

    .item-name {
        font-weight: 600;
        color: #1f2937;
        margin: 0 0 0.25rem 0;
        font-size: 1rem;
    }

    .item-name a {
        color: #eb5202;
        text-decoration: none;
        transition: color 0.2s;
    }

    .item-name a:hover {
        color: #f38b00;
        text-decoration: underline;
    }

    .item-meta {
        color: #9ca3af;
        font-size: 0.875rem;
        margin: 0;
    }

    .item-count {
        background: linear-gradient(135deg, #eb5202 0%, #f38b00 100%);
        color: white;
        padding: 0.5rem 1rem;
        border-radius: 0.5rem;
        font-weight: 700;
        font-size: 1.25rem;
        min-width: 60px;
        text-align: center;
    }

    .rank-badge {
        background: linear-gradient(135deg, #f59e0b 0%, #ef4444 100%);
        color: white;
        width: 2rem;
        height: 2rem;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 700;
        margin-right: 1rem;
        font-size: 0.875rem;
    }

    .rank-badge.gold { background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%); }
    .rank-badge.silver { background: linear-gradient(135deg, #94a3b8 0%, #64748b 100%); }
    .rank-badge.bronze { background: linear-gradient(135deg, #d97706 0%, #92400e 100%); }

    .time-badge {
        background: #e5e7eb;
        color: #6b7280;
        padding: 0.25rem 0.75rem;
        border-radius: 9999px;
        font-size: 0.75rem;
        font-weight: 600;
        white-space: nowrap;
    }

    .empty-state {
        text-align: center;
        padding: 3rem 1rem;
        color: #9ca3af;
    }

    .empty-state svg {
        width: 4rem;
        height: 4rem;
        margin: 0 auto 1rem;
        opacity: 0.3;
    }

    canvas {
        max-height: 300px;
    }

    .full-width {
        grid-column: 1 / -1;
    }

    .detail-link {
        color: #eb5202;
        text-decoration: none;
        transition: color 0.2s;
    }

    .detail-link:hover {
        color: #f38b00;
        text-decoration: underline;
    }
</style>

<div class="view-tracker-container">
    <!-- Header -->
    <div class="tracker-header">
        <h1>📊 Product View Tracker</h1>
        <p>Comprehensive analytics and insights for product views across your store</p>
    </div>

    <!-- Statistics Cards -->
    <div class="stats-grid">
        <div class="stat-card purple">
            <div class="stat-icon">👁️</div>
            <p class="stat-label">Total Views</p>
            <div class="stat-value"><?= $escaper->escapeHtml(number_format($stats['total'])) ?></div>
        </div>

        <div class="stat-card blue">
            <div class="stat-icon">📅</div>
            <p class="stat-label">Today's Views</p>
            <div class="stat-value"><?= $escaper->escapeHtml(number_format($stats['today'])) ?></div>
        </div>

        <div class="stat-card green">
            <div class="stat-icon">📊</div>
            <p class="stat-label">This Week</p>
            <div class="stat-value"><?= $escaper->escapeHtml(number_format($stats['week'])) ?></div>
        </div>

        <div class="stat-card orange">
            <div class="stat-icon">📈</div>
            <p class="stat-label">This Month</p>
            <div class="stat-value"><?= $escaper->escapeHtml(number_format($stats['month'])) ?></div>
        </div>

        <div class="stat-card pink">
            <div class="stat-icon">👥</div>
            <p class="stat-label">Unique Visitors</p>
            <div class="stat-value"><?= $escaper->escapeHtml(number_format($stats['unique_visitors'])) ?></div>
        </div>
    </div>

    <!-- Charts Section -->
    <div class="content-grid">
        <div class="chart-card full-width">
            <div class="card-header">
                <h2 class="card-title">📈 30-Day View Trends</h2>
                <span class="badge">Last 30 Days</span>
            </div>
            <canvas id="trendChart"></canvas>
        </div>
    </div>

    <div class="content-grid">
        <div class="chart-card">
            <div class="card-header">
                <h2 class="card-title">🕐 Today's Hourly Distribution</h2>
                <span class="badge">Real-time</span>
            </div>
            <canvas id="hourlyChart"></canvas>
        </div>

        <div class="list-card">
            <div class="card-header">
                <h2 class="card-title">🏆 Top Customers</h2>
                <span class="badge"><?= (int)count($topCustomers) ?></span>
            </div>
            <?php if (!empty($topCustomers)): ?>
                <ul class="customer-list">
                    <?php $rank = 1; foreach ($topCustomers as $customer): ?>
                        <li class="customer-item">
                            <span class="rank-badge <?= $escaper->escapeHtmlAttr($rank === 1 ? 'gold' : ($rank === 2 ? 'silver' : ($rank === 3 ? 'bronze' : ''))) ?>">
                                <?= (int)$rank ?>
                            </span>
                            <div class="item-info">
                                <p class="item-name">
                                    <a href="<?= $escaper->escapeUrl($customer['url']) ?>" target="_blank">
                                        <?= $escaper->escapeHtml($customer['name']) ?>
                                    </a>
                                </p>
                                <p class="item-meta"><?= $escaper->escapeHtml($customer['email']) ?></p>
                            </div>
                            <div class="item-count"><?= $escaper->escapeHtml(number_format($customer['count'])) ?></div>
                        </li>
                    <?php $rank++; endforeach; ?>
                </ul>
            <?php else: ?>
                <div class="empty-state">
                    <p>No customer data available</p>
                </div>
            <?php endif; ?>
        </div>
    </div>

    <!-- Most Viewed Products -->
    <div class="content-grid">
        <div class="list-card">
            <div class="card-header">
                <h2 class="card-title">🔥 Most Viewed Products</h2>
                <span class="badge"><?= (int)count($mostViewed) ?></span>
            </div>
            <?php if (!empty($mostViewed)): ?>
                <ul class="product-list">
                    <?php $rank = 1; foreach ($mostViewed as $product): ?>
                        <li class="product-item">
                            <span class="rank-badge <?= $escaper->escapeHtmlAttr($rank === 1 ? 'gold' : ($rank === 2 ? 'silver' : ($rank === 3 ? 'bronze' : ''))) ?>">
                                <?= (int)$rank ?>
                            </span>
                            <div class="item-info">
                                <p class="item-name">
                                    <a href="<?= $escaper->escapeUrl($product['url']) ?>" target="_blank">
                                        <?= $escaper->escapeHtml($product['name']) ?>
                                    </a>
                                </p>
                                <p class="item-meta">SKU: <?= $escaper->escapeHtml($product['sku']) ?></p>
                            </div>
                            <div class="item-count"><?= $escaper->escapeHtml(number_format($product['count'])) ?></div>
                        </li>
                    <?php $rank++; endforeach; ?>
                </ul>
            <?php else: ?>
                <div class="empty-state">
                    <p>No product views tracked yet</p>
                </div>
            <?php endif; ?>
        </div>

        <!-- Recent Views -->
        <div class="list-card">
            <div class="card-header">
                <h2 class="card-title">⏱️ Recent Views</h2>
                <span class="badge">Live</span>
            </div>
            <?php if (!empty($recentViews)): ?>
                <ul class="recent-list">
                    <?php foreach (array_slice($recentViews, 0, 10) as $view): ?>
                        <li class="recent-item">
                            <div class="item-info">
                                <p class="item-name">
                                    <a href="<?= $escaper->escapeUrl($view['product_url']) ?>" target="_blank">
                                        <?= $escaper->escapeHtml($view['product_name']) ?>
                                    </a>
                                </p>
                                <p class="item-meta">
                                    <?= $escaper->escapeHtml($view['viewer_name']) ?> •
                                    SKU: <?= $escaper->escapeHtml($view['product_sku']) ?>
                                </p>
                            </div>
                            <div style="display: flex; align-items: center; gap: 0.75rem;">
                                <a href="<?= $escaper->escapeUrl($block->getUrl('quickview/viewtracker/view', ['id' => $view['view_id']])) ?>"
                                   class="detail-link" style="font-size: 0.875rem; font-weight: 600;">
                                    View Details
                                </a>
                                <span class="time-badge"><?= $escaper->escapeHtml($view['time_ago']) ?></span>
                            </div>
                        </li>
                    <?php endforeach; ?>
                </ul>
            <?php else: ?>
                <div class="empty-state">
                    <p>No recent views</p>
                </div>
            <?php endif; ?>
        </div>
    </div>
</div>

<script>
require(['jquery', 'chartjs', 'domReady!'], function($, Chart) {
    // Trend Chart
    const trendCtx = document.getElementById('trendChart');
    if (trendCtx) {
        const trendChart = new Chart(trendCtx.getContext('2d'), {
            type: 'line',
            data: {
                labels: <?= /* @noEscape */ json_encode($trendData['labels']) ?>,
                datasets: [
                    {
                        label: 'Total Views',
                        data: <?= /* @noEscape */ json_encode($trendData['view_data']) ?>,
                        borderColor: '#eb5202',
                        backgroundColor: 'rgba(139, 92, 246, 0.1)',
                        borderWidth: 3,
                        fill: true,
                        tension: 0.4,
                        pointRadius: 4,
                        pointHoverRadius: 6
                    },
                    {
                        label: 'Unique Products',
                        data: <?= /* @noEscape */ json_encode($trendData['unique_data']) ?>,
                        borderColor: '#3b82f6',
                        backgroundColor: 'rgba(59, 130, 246, 0.1)',
                        borderWidth: 3,
                        fill: true,
                        tension: 0.4,
                        pointRadius: 4,
                        pointHoverRadius: 6
                    }
                ]
            },
            options: {
                responsive: true,
                maintainAspectRatio: true,
                plugins: {
                    legend: {
                        display: true,
                        position: 'top',
                        labels: {
                            font: {
                                size: 14,
                                weight: 'bold'
                            },
                            padding: 20
                        }
                    },
                    tooltip: {
                        backgroundColor: 'rgba(0, 0, 0, 0.8)',
                        padding: 12,
                        titleFont: {
                            size: 14
                        },
                        bodyFont: {
                            size: 13
                        }
                    }
                },
                scales: {
                    y: {
                        beginAtZero: true,
                        grid: {
                            color: '#f3f4f6'
                        },
                        ticks: {
                            font: {
                                size: 12
                            }
                        }
                    },
                    x: {
                        grid: {
                            display: false
                        },
                        ticks: {
                            font: {
                                size: 12
                            }
                        }
                    }
                }
            }
        });
    }

    // Hourly Chart
    const hourlyCtx = document.getElementById('hourlyChart');
    if (hourlyCtx) {
        const hourlyChart = new Chart(hourlyCtx.getContext('2d'), {
            type: 'bar',
            data: {
                labels: <?= /* @noEscape */ json_encode($hourlyData['labels']) ?>,
                datasets: [{
                    label: 'Views',
                    data: <?= /* @noEscape */ json_encode($hourlyData['data']) ?>,
                    backgroundColor: 'rgba(139, 92, 246, 0.8)',
                    borderColor: '#eb5202',
                    borderWidth: 2,
                    borderRadius: 8
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: true,
                plugins: {
                    legend: {
                        display: false
                    },
                    tooltip: {
                        backgroundColor: 'rgba(0, 0, 0, 0.8)',
                        padding: 12,
                        titleFont: {
                            size: 14
                        },
                        bodyFont: {
                            size: 13
                        }
                    }
                },
                scales: {
                    y: {
                        beginAtZero: true,
                        grid: {
                            color: '#f3f4f6'
                        },
                        ticks: {
                            font: {
                                size: 11
                            }
                        }
                    },
                    x: {
                        grid: {
                            display: false
                        },
                        ticks: {
                            font: {
                                size: 10
                            },
                            maxRotation: 45,
                            minRotation: 45
                        }
                    }
                }
            }
        });
    }
});
</script>
